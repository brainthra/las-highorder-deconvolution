# lasim
`under construction`

Python package for <u>L</u>inear <u>A</u>bsorption <u>S</u>pectrometer s<u>im</u>ulation.

> **Note**: This package uses `MACR` to obtain crosssections.

## Installation

now to install `lasim`, go to the package directory root, you must be at the same level as `pyproject.toml` and run:
```bash
pip install -e .
```