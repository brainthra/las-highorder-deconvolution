# laspectron

<u>L</u>inear <u>A</u>bsorption <u>S</u>pectrometer s<u>pectr</u>a reconstructi<u>on</u>

package to reconstruct spectra from LAS measured data.

## Installation

now to install `laspectron`, go to the package directory root, you must be at the same level as `pyproject.toml` and run:
```bash
pip install -e .
```